'use client';

// Adapter: the actual dashboard UI lives in `app/page.js` during migration.
// Once the split is complete, this file can host the full dashboard implementation.
export { DashboardView } from '../../app/page.js';

